<Window x:Class="GameTranslaterUI.WindowTrans"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameTranslaterUI"
        mc:Ignorable="d"
        Title="WindowTrans" Height="355.661" Width="554.154">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="171*"/>
            <RowDefinition Height="172*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="textBox" Margin="10,0,0,10" TextWrapping="Wrap" Text="TextBox" Height="67" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="104" Grid.Row="1"/>
        <DataGrid x:Name="dataGrid" Margin="119,31,10,10" Grid.RowSpan="2"/>
        <Menu x:Name="menu" Margin="10,10,10,0" Height="16" VerticalAlignment="Top">
            <MenuItem Header="Test">
                <MenuItem Command="ApplicationCommands.Copy"/>
            </MenuItem>
        </Menu>
        <Button x:Name="button_Export" Content="输出至文件" HorizontalAlignment="Left" Margin="21,123,0,0" VerticalAlignment="Top" Width="75" Click="button_Export_Click" Height="20"/>
        <Button x:Name="button_Import" HorizontalAlignment="Left" Margin="21,85,0,0" VerticalAlignment="Top" Width="75" Click="button_Import_Click" Height="20" Content="从文件导入"/>
        <ListView ItemsSource="{Binding}" x:Name="listView_Plugs" HorizontalAlignment="Left" Width="75" SelectionChanged="listView_Plugs_SelectionChanged" Margin="30,10,0,41" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ID}"  Width="30" />
                    <GridViewColumn Header="属性" DisplayMemberBinding="{Binding Name}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>


    </Grid>
</Window>
